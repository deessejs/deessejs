---
"@deessejs/cli": major
---

Initial public release of `@deessejs/cli`. Bump version `0.1.0 → 1.0.0` (the npm namespace `@deessejs/cli` was previously claimed up to `0.6.46` from earlier unrelated work). Establish the single-workflow release pattern.
