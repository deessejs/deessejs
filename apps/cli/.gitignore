# Local artifacts that should not be committed.
# The CLI tarball is generated by `npm pack` and consumed by the e2e
# suite via the DEESSEJS_CLI_TARBALL env var. It is a build artifact,
# not source.
deessejs-cli-*.tgz
