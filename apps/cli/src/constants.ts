/**
 * Default API base URL. The oRPC client appends the procedure path
 * (e.g. `<baseUrl>/templates/list`) automatically.
 */
export const DEFAULT_API_URL = "https://app.deessejs.com/api/v1/rpc"

export const USER_AGENT = "deessejs-cli/0.1.0 (https://deessejs.com)"

export const EXIT_SUCCESS = 0
export const EXIT_ERROR = 1
