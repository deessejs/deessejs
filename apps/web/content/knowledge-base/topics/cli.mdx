---
title: CLI
description: deessejs CLI commands, registry, scaffolding.
order: 5
---

Guides in this topic cover the `deessejs` CLI — the
`init`, `list`, and `info` commands, the template registry,
and the auth flow. Start with "Install the deessejs CLI"
if you have never run it before.