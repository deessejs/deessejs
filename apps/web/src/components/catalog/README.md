# Catalog

Shared surface for the /components and /blocks registries. Both routes
use the same primitives (cards, grids, sidebars, preview tabs, code
panels) and the same data shape (CatalogItem, CatalogCategory).

Out of scope: apps/web/src/registry/* is the shadcn-registry JSON
publisher for consumer install. It is unrelated to these marketing
catalogues.
