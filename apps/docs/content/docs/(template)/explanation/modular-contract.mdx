---
title: The modular contract
description: What every feature module must satisfy — and why this enables the delete guarantee.
---

*[Explanation. The contract: every feature is self-contained, has a delete test, registers via a single import surface, and the CI runs delete tests on every PR.]*